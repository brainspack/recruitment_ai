import authenticationModel from './Authentication';
import adminModel from './Admin';
import classimizeModel from './Classimize';

const storeModel = {
  authentication: authenticationModel,
  admin: adminModel,
  classimize: classimizeModel,
};

export default storeModel;
